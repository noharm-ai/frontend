import { FloatButton } from "antd";
import styled from "styled-components/macro";

export const FloatButtonGroup = styled(FloatButton.Group)`
  > button {
    .ant-float-btn-body {
      background: #a991d6;
    }
  }
`;
