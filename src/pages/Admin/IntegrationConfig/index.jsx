import withLayout from "lib/withLayout";
import IntegrationConfig from "features/admin/IntegrationConfig/IntegrationConfig";

const layoutProps = {};
const IntegrationConfigWithLayout = withLayout(IntegrationConfig, layoutProps);

export default IntegrationConfigWithLayout;
