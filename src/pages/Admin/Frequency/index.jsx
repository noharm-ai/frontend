import withLayout from "lib/withLayout";
import Frequency from "features/admin/Frequency/Frequency";

const layoutProps = {};
const FrequencyWithLayout = withLayout(Frequency, layoutProps);

export default FrequencyWithLayout;
