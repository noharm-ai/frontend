import withLayout from "lib/withLayout";
import Memory from "features/admin/Memory/Memory";

const layoutProps = {};

const MemoryWithLayout = withLayout(Memory, layoutProps);

export default MemoryWithLayout;
