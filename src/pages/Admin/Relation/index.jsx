import withLayout from "lib/withLayout";
import Relation from "features/admin/Relation/Relation";

const layoutProps = {};

const RelationWithLayout = withLayout(Relation, layoutProps);

export default RelationWithLayout;
