import withLayout from "src/lib/withLayout";
import { ProtocolEditorPage } from "features/admin/Protocol/Form/ProtocolEditorPage";

const ProtocolEditorWithLayout = withLayout(ProtocolEditorPage, {});

export default ProtocolEditorWithLayout;
