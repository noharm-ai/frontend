import withLayout from "lib/withLayout";
import AdminSegment from "features/admin/Segment/Segment";

const layoutProps = {};

const AdminSegmentWithLayout = withLayout(AdminSegment, layoutProps);

export default AdminSegmentWithLayout;
