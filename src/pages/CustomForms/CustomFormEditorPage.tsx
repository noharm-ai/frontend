import withLayout from "src/lib/withLayout";
import CustomFormEditorPage from "features/memory/CustomForms/CustomFormEditorPage";

const CustomFormEditorWithLayout = withLayout(CustomFormEditorPage, {});

export default CustomFormEditorWithLayout;
